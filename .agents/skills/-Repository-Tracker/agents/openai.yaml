interface:
  display_name: "Repository Tracker"
  short_description: "Repo-specific patterns and workflows for -Repository-Tracker"
  default_prompt: "Use the -Repository-Tracker repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true